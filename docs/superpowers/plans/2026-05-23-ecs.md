# ECS (EC2) 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** EKS 아티팩트를 제거하고 Amazon ECS on EC2로 Node.js WebSocket 서버를 배포. AWS CLI로 인프라 직접 구성.

**Architecture:** ECR 이미지 → ECS Task Definition → ECS Service (awsvpc, rolling update) → ALB Target Group. EC2 ASG가 ECS Capacity Provider로 동작. Application Auto Scaling이 CPU 70% 기준으로 Task 수 조절.

**Tech Stack:** AWS CLI (profile: dsu-roleswitch), Docker, ECS, ECR, ALB, ElastiCache Redis (기존), S3 (기존)

**전제조건:**
- AWS CLI 인증: `aws sts get-caller-identity --profile dsu-roleswitch`
- Docker 실행 중: `docker info`
- ECR 레포 존재: `026951011097.dkr.ecr.ap-northeast-2.amazonaws.com/holdem-server`
- `infra/required-iam-permissions.json`의 ECS 권한이 `role-dsu-roleswitch`에 부여된 상태

---

## File Structure

```
infra/
├── ecs/
│   ├── 01-iam.sh           ← 신규 생성
│   ├── 02-cluster.sh       ← 신규 생성
│   ├── 03-task-def.sh      ← 신규 생성
│   ├── 04-alb.sh           ← 신규 생성
│   └── 05-service.sh       ← 신규 생성
├── required-iam-permissions.json   ← ECS 권한으로 교체
└── eks-cluster.yaml        ← 제거

k8s/                        ← 디렉토리 전체 제거
```

---

### Task 1: EKS 아티팩트 정리

**Files:**
- Delete: `k8s/` 디렉토리 전체
- Delete: `infra/eks-cluster.yaml`
- Modify: `docs/superpowers/plans/2026-05-23-kubernetes.md` → 폐기 표시

- [ ] **Step 1: k8s/ 디렉토리 제거**

```bash
git rm -r k8s/
```

Expected:
```
rm 'k8s/aws-lbc/service-account.yaml'
rm 'k8s/namespace.yaml'
rm 'k8s/server/deployment.yaml'
rm 'k8s/server/hpa.yaml'
rm 'k8s/server/ingress.yaml'
rm 'k8s/server/service.yaml'
```

- [ ] **Step 2: eks-cluster.yaml 제거**

```bash
git rm infra/eks-cluster.yaml
```

- [ ] **Step 3: EKS 플랜 폐기 표시**

`docs/superpowers/plans/2026-05-23-kubernetes.md` 첫 줄 아래에 추가:

```markdown
> ⚠️ **DEPRECATED (2026-05-23):** ECS로 전환. `docs/superpowers/plans/2026-05-23-ecs.md` 참고.
```

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "chore: remove EKS artifacts, switch to ECS"
```

---

### Task 2: IAM 권한 파일 ECS로 교체

**Files:**
- Modify: `infra/required-iam-permissions.json`

- [ ] **Step 1: 파일 전체 교체**

`infra/required-iam-permissions.json`:
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ECSFull",
      "Effect": "Allow",
      "Action": ["ecs:*"],
      "Resource": "*"
    },
    {
      "Sid": "ECRForECS",
      "Effect": "Allow",
      "Action": [
        "ecr:GetAuthorizationToken",
        "ecr:BatchCheckLayerAvailability",
        "ecr:GetDownloadUrlForLayer",
        "ecr:BatchGetImage",
        "ecr:CreateRepository",
        "ecr:DescribeRepositories",
        "ecr:PutImage",
        "ecr:InitiateLayerUpload",
        "ecr:UploadLayerPart",
        "ecr:CompleteLayerUpload"
      ],
      "Resource": "*"
    },
    {
      "Sid": "IAMForECS",
      "Effect": "Allow",
      "Action": [
        "iam:CreateRole", "iam:DeleteRole",
        "iam:AttachRolePolicy", "iam:DetachRolePolicy",
        "iam:PutRolePolicy", "iam:DeleteRolePolicy",
        "iam:GetRole", "iam:GetRolePolicy",
        "iam:ListRolePolicies", "iam:ListAttachedRolePolicies",
        "iam:CreateInstanceProfile", "iam:DeleteInstanceProfile",
        "iam:AddRoleToInstanceProfile", "iam:RemoveRoleFromInstanceProfile",
        "iam:GetInstanceProfile", "iam:PassRole",
        "iam:CreateServiceLinkedRole",
        "iam:TagRole", "iam:UntagRole"
      ],
      "Resource": "*"
    },
    {
      "Sid": "EC2ForECS",
      "Effect": "Allow",
      "Action": [
        "ec2:CreateSecurityGroup", "ec2:DeleteSecurityGroup",
        "ec2:AuthorizeSecurityGroupIngress", "ec2:RevokeSecurityGroupIngress",
        "ec2:AuthorizeSecurityGroupEgress", "ec2:RevokeSecurityGroupEgress",
        "ec2:DescribeSecurityGroups", "ec2:DescribeSecurityGroupRules",
        "ec2:DescribeVpcs", "ec2:DescribeSubnets",
        "ec2:DescribeInstances", "ec2:DescribeInstanceTypes",
        "ec2:DescribeAvailabilityZones", "ec2:DescribeTags",
        "ec2:DescribeImages", "ec2:DescribeKeyPairs",
        "ec2:DescribeNetworkInterfaces",
        "ec2:DescribeLaunchTemplates", "ec2:DescribeLaunchTemplateVersions",
        "ec2:CreateLaunchTemplate", "ec2:DeleteLaunchTemplate",
        "ec2:CreateLaunchTemplateVersion",
        "ec2:RunInstances", "ec2:TerminateInstances",
        "ec2:CreateTags"
      ],
      "Resource": "*"
    },
    {
      "Sid": "ELBForECS",
      "Effect": "Allow",
      "Action": [
        "elasticloadbalancing:CreateLoadBalancer",
        "elasticloadbalancing:DeleteLoadBalancer",
        "elasticloadbalancing:DescribeLoadBalancers",
        "elasticloadbalancing:ModifyLoadBalancerAttributes",
        "elasticloadbalancing:DescribeLoadBalancerAttributes",
        "elasticloadbalancing:CreateTargetGroup",
        "elasticloadbalancing:DeleteTargetGroup",
        "elasticloadbalancing:DescribeTargetGroups",
        "elasticloadbalancing:ModifyTargetGroup",
        "elasticloadbalancing:ModifyTargetGroupAttributes",
        "elasticloadbalancing:RegisterTargets",
        "elasticloadbalancing:DeregisterTargets",
        "elasticloadbalancing:DescribeTargetHealth",
        "elasticloadbalancing:CreateListener",
        "elasticloadbalancing:DeleteListener",
        "elasticloadbalancing:DescribeListeners",
        "elasticloadbalancing:AddTags",
        "elasticloadbalancing:DescribeTags"
      ],
      "Resource": "*"
    },
    {
      "Sid": "AutoScalingForECS",
      "Effect": "Allow",
      "Action": [
        "autoscaling:CreateAutoScalingGroup", "autoscaling:UpdateAutoScalingGroup",
        "autoscaling:DeleteAutoScalingGroup", "autoscaling:DescribeAutoScalingGroups",
        "autoscaling:DescribeAutoScalingInstances",
        "autoscaling:PutScalingPolicy", "autoscaling:DeletePolicy",
        "autoscaling:DescribePolicies", "autoscaling:DescribeScalingActivities",
        "autoscaling:SetDesiredCapacity",
        "autoscaling:TerminateInstanceInAutoScalingGroup",
        "autoscaling:EnableMetricsCollection",
        "autoscaling:CreateOrUpdateTags"
      ],
      "Resource": "*"
    },
    {
      "Sid": "AppAutoScalingForECS",
      "Effect": "Allow",
      "Action": [
        "application-autoscaling:RegisterScalableTarget",
        "application-autoscaling:DeregisterScalableTarget",
        "application-autoscaling:PutScalingPolicy",
        "application-autoscaling:DeleteScalingPolicy",
        "application-autoscaling:DescribeScalableTargets",
        "application-autoscaling:DescribeScalingPolicies"
      ],
      "Resource": "*"
    },
    {
      "Sid": "ElastiCacheForECS",
      "Effect": "Allow",
      "Action": [
        "elasticache:CreateCacheCluster", "elasticache:DeleteCacheCluster",
        "elasticache:DescribeCacheClusters", "elasticache:ModifyCacheCluster",
        "elasticache:CreateCacheSubnetGroup", "elasticache:DeleteCacheSubnetGroup",
        "elasticache:DescribeCacheSubnetGroups",
        "elasticache:AddTagsToResource", "elasticache:ListTagsForResource"
      ],
      "Resource": "*"
    },
    {
      "Sid": "CloudWatchForECS",
      "Effect": "Allow",
      "Action": [
        "cloudwatch:PutMetricAlarm", "cloudwatch:DeleteAlarms",
        "cloudwatch:DescribeAlarms",
        "logs:CreateLogGroup", "logs:CreateLogStream",
        "logs:PutLogEvents", "logs:DescribeLogGroups",
        "logs:DescribeLogStreams"
      ],
      "Resource": "*"
    },
    {
      "Sid": "SSMForAMILookup",
      "Effect": "Allow",
      "Action": ["ssm:GetParameters", "ssm:GetParameter"],
      "Resource": "*"
    }
  ]
}
```

- [ ] **Step 2: 커밋**

```bash
git add infra/required-iam-permissions.json
git commit -m "chore: replace EKS IAM permissions with ECS permissions"
```

---

### Task 3: 보안 그룹 생성

**Files:**
- Create: `infra/ecs/03-security-groups.sh` (참고용, 실제 실행은 직접)

- [ ] **Step 1: ALB 보안 그룹 생성**

```bash
ALB_SG=$(aws ec2 create-security-group \
  --group-name holdem-alb-sg \
  --description "Allow HTTP from internet to ALB" \
  --vpc-id vpc-0e4283922b93ba6ea \
  --profile dsu-roleswitch \
  --query 'GroupId' --output text)
echo "ALB SG: $ALB_SG"
```

Expected: `sg-xxxxxxxxxxxxxxxxx`

- [ ] **Step 2: ALB 보안 그룹 인바운드 규칙 (80 허용)**

```bash
aws ec2 authorize-security-group-ingress \
  --group-id $ALB_SG \
  --protocol tcp --port 80 --cidr 0.0.0.0/0 \
  --profile dsu-roleswitch
```

- [ ] **Step 3: EC2 보안 그룹 생성**

```bash
EC2_SG=$(aws ec2 create-security-group \
  --group-name holdem-ec2-sg \
  --description "Allow 3001 from ALB only" \
  --vpc-id vpc-0e4283922b93ba6ea \
  --profile dsu-roleswitch \
  --query 'GroupId' --output text)
echo "EC2 SG: $EC2_SG"
```

- [ ] **Step 4: EC2 보안 그룹 인바운드 규칙 (ALB SG에서 3001)**

```bash
aws ec2 authorize-security-group-ingress \
  --group-id $EC2_SG \
  --protocol tcp --port 3001 \
  --source-group $ALB_SG \
  --profile dsu-roleswitch
```

- [ ] **Step 5: Redis 보안 그룹 생성**

```bash
REDIS_SG=$(aws ec2 create-security-group \
  --group-name holdem-redis-sg \
  --description "Allow 6379 from EC2 only" \
  --vpc-id vpc-0e4283922b93ba6ea \
  --profile dsu-roleswitch \
  --query 'GroupId' --output text)
echo "Redis SG: $REDIS_SG"
```

- [ ] **Step 6: Redis 보안 그룹 인바운드 규칙 (EC2 SG에서 6379)**

```bash
aws ec2 authorize-security-group-ingress \
  --group-id $REDIS_SG \
  --protocol tcp --port 6379 \
  --source-group $EC2_SG \
  --profile dsu-roleswitch
```

- [ ] **Step 7: 생성 확인**

```bash
aws ec2 describe-security-groups \
  --filters "Name=group-name,Values=holdem-alb-sg,holdem-ec2-sg,holdem-redis-sg" \
  --query 'SecurityGroups[*].{Name:GroupName,Id:GroupId}' \
  --output table \
  --profile dsu-roleswitch
```

Expected: 3개 보안 그룹 모두 표시

---

### Task 4: IAM Role 생성

**Files:**
- Create: `infra/ecs/01-iam.sh`

- [ ] **Step 1: 01-iam.sh 작성**

`infra/ecs/01-iam.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch
ACCOUNT_ID=026951011097
HISTORY_BUCKET=holdem-history-026951011097

# ECS Task Role (컨테이너 → S3 쓰기)
aws iam create-role \
  --role-name holdem-ecs-task-role \
  --assume-role-policy-document '{
    "Version": "2012-10-17",
    "Statement": [{
      "Effect": "Allow",
      "Principal": {"Service": "ecs-tasks.amazonaws.com"},
      "Action": "sts:AssumeRole"
    }]
  }' \
  --profile $PROFILE

aws iam put-role-policy \
  --role-name holdem-ecs-task-role \
  --policy-name holdem-s3-history-write \
  --policy-document "{
    \"Version\": \"2012-10-17\",
    \"Statement\": [{
      \"Effect\": \"Allow\",
      \"Action\": [\"s3:PutObject\"],
      \"Resource\": \"arn:aws:s3:::${HISTORY_BUCKET}/*\"
    }]
  }" \
  --profile $PROFILE

echo "Task Role ARN: arn:aws:iam::${ACCOUNT_ID}:role/holdem-ecs-task-role"

# ECS Execution Role (ECS → ECR pull, CloudWatch 로그 쓰기)
# 신뢰 주체: ecs-tasks.amazonaws.com (instance role과 다름)
aws iam create-role \
  --role-name holdem-ecs-execution-role \
  --assume-role-policy-document '{
    "Version": "2012-10-17",
    "Statement": [{
      "Effect": "Allow",
      "Principal": {"Service": "ecs-tasks.amazonaws.com"},
      "Action": "sts:AssumeRole"
    }]
  }' \
  --profile $PROFILE

aws iam attach-role-policy \
  --role-name holdem-ecs-execution-role \
  --policy-arn arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy \
  --profile $PROFILE

echo "Execution Role ARN: arn:aws:iam::${ACCOUNT_ID}:role/holdem-ecs-execution-role"

# ECS Instance Role (EC2 → ECS 등록, ECR pull)
aws iam create-role \
  --role-name holdem-ecs-instance-role \
  --assume-role-policy-document '{
    "Version": "2012-10-17",
    "Statement": [{
      "Effect": "Allow",
      "Principal": {"Service": "ec2.amazonaws.com"},
      "Action": "sts:AssumeRole"
    }]
  }' \
  --profile $PROFILE

aws iam attach-role-policy \
  --role-name holdem-ecs-instance-role \
  --policy-arn arn:aws:iam::aws:policy/service-role/AmazonEC2ContainerServiceforEC2Role \
  --profile $PROFILE

aws iam attach-role-policy \
  --role-name holdem-ecs-instance-role \
  --policy-arn arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly \
  --profile $PROFILE

# Instance Profile (EC2에 Role 연결하려면 필요)
aws iam create-instance-profile \
  --instance-profile-name holdem-ecs-instance-profile \
  --profile $PROFILE

aws iam add-role-to-instance-profile \
  --instance-profile-name holdem-ecs-instance-profile \
  --role-name holdem-ecs-instance-role \
  --profile $PROFILE

echo "Instance Role ARN: arn:aws:iam::${ACCOUNT_ID}:role/holdem-ecs-instance-role"
echo "Done: IAM roles created"
```

- [ ] **Step 2: 실행**

```bash
chmod +x infra/ecs/01-iam.sh && bash infra/ecs/01-iam.sh
```

Expected 마지막 줄: `Done: IAM roles created`

- [ ] **Step 3: 확인**

```bash
aws iam get-role --role-name holdem-ecs-task-role --profile dsu-roleswitch --query 'Role.RoleName' --output text
aws iam get-role --role-name holdem-ecs-execution-role --profile dsu-roleswitch --query 'Role.RoleName' --output text
aws iam get-role --role-name holdem-ecs-instance-role --profile dsu-roleswitch --query 'Role.RoleName' --output text
```

Expected: `holdem-ecs-task-role`, `holdem-ecs-execution-role`, `holdem-ecs-instance-role` 각각 출력

- [ ] **Step 4: 커밋**

```bash
git add infra/ecs/01-iam.sh
git commit -m "feat: add ECS IAM roles (task role, instance role)"
```

---

### Task 5: ElastiCache Redis 생성

> ⚠️ 권한 필요: `elasticache:*`. 없으면 IAM 관리자에게 `required-iam-permissions.json`의 `ElastiCacheForECS` 정책 추가 요청.

**Files:**
- Create: `infra/ecs/00-redis.sh`

- [ ] **Step 1: Redis 존재 여부 확인**

```bash
aws elasticache describe-cache-clusters \
  --cache-cluster-id holdem-redis \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'CacheClusters[0].CacheClusterStatus' \
  --output text 2>&1
```

Expected: `available` → Task 5 건너뜀.  
Expected: `An error occurred` → 아래 단계 진행.

- [ ] **Step 2: 00-redis.sh 작성**

`infra/ecs/00-redis.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch

# Redis 서브넷 그룹
aws elasticache create-cache-subnet-group \
  --cache-subnet-group-name holdem-redis-subnet \
  --cache-subnet-group-description "Cloud Holdem Redis subnet group" \
  --subnet-ids subnet-0dd262b44669174f8 subnet-04bea6a2e3d9da9f5 subnet-02e3f5e14bf2a7282 \
  --profile $PROFILE

REDIS_SG=$(aws ec2 describe-security-groups \
  --filters "Name=group-name,Values=holdem-redis-sg" \
  --query 'SecurityGroups[0].GroupId' --output text \
  --profile $PROFILE)

aws elasticache create-cache-cluster \
  --cache-cluster-id holdem-redis \
  --cache-node-type cache.t3.micro \
  --engine redis \
  --engine-version 7.1 \
  --num-cache-nodes 1 \
  --cache-subnet-group-name holdem-redis-subnet \
  --security-group-ids $REDIS_SG \
  --region ap-northeast-2 \
  --profile $PROFILE

echo "Waiting for Redis cluster to be available (~5min)..."
aws elasticache wait cache-cluster-available \
  --cache-cluster-id holdem-redis \
  --region ap-northeast-2 \
  --profile $PROFILE

REDIS_HOST=$(aws elasticache describe-cache-clusters \
  --cache-cluster-id holdem-redis \
  --show-cache-node-info \
  --region ap-northeast-2 \
  --profile $PROFILE \
  --query 'CacheClusters[0].CacheNodes[0].Endpoint.Address' \
  --output text)

echo "Redis endpoint: $REDIS_HOST"
```

- [ ] **Step 3: 실행**

```bash
chmod +x infra/ecs/00-redis.sh && bash infra/ecs/00-redis.sh
```

Expected 마지막 줄: `Redis endpoint: holdem-redis.xxxxxx.0001.apn2.cache.amazonaws.com`

- [ ] **Step 4: Redis 엔드포인트 저장 (이후 Task에서 사용)**

```bash
REDIS_HOST=$(aws elasticache describe-cache-clusters \
  --cache-cluster-id holdem-redis \
  --show-cache-node-info \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'CacheClusters[0].CacheNodes[0].Endpoint.Address' \
  --output text)
echo "REDIS_HOST=$REDIS_HOST"
```

- [ ] **Step 5: 커밋**

```bash
git add infra/ecs/00-redis.sh
git commit -m "feat: add ElastiCache Redis creation script"
```

---

### Task 6: ECR 이미지 빌드 & Push

**Files:**
- 없음 (기존 Dockerfile 사용)

- [ ] **Step 1: ECR 로그인**

```bash
aws ecr get-login-password \
  --region ap-northeast-2 \
  --profile dsu-roleswitch | \
docker login \
  --username AWS \
  --password-stdin \
  026951011097.dkr.ecr.ap-northeast-2.amazonaws.com
```

Expected: `Login Succeeded`

- [ ] **Step 2: 이미지 빌드 & 태깅**

```bash
docker build -t holdem-server:latest ./server

docker tag holdem-server:latest \
  026951011097.dkr.ecr.ap-northeast-2.amazonaws.com/holdem-server:latest
```

- [ ] **Step 3: ECR push**

```bash
docker push \
  026951011097.dkr.ecr.ap-northeast-2.amazonaws.com/holdem-server:latest
```

Expected: `latest: digest: sha256:xxxx size: xxxx`

- [ ] **Step 4: 확인**

```bash
aws ecr describe-images \
  --repository-name holdem-server \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'imageDetails[0].{Tag:imageTags[0],PushedAt:imagePushedAt}' \
  --output table
```

Expected: `latest` 태그와 현재 시간 표시

---

### Task 7: ECS 클러스터 & ASG 생성

**Files:**
- Create: `infra/ecs/02-cluster.sh`

- [ ] **Step 1: ECS-optimized AMI ID 조회**

```bash
ECS_AMI=$(aws ssm get-parameters \
  --names /aws/service/ecs/optimized-ami/amazon-linux-2023/recommended/image_id \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'Parameters[0].Value' \
  --output text)
echo "ECS AMI: $ECS_AMI"
```

Expected: `ami-xxxxxxxxxxxxxxxxx`

- [ ] **Step 2: 02-cluster.sh 작성**

`infra/ecs/02-cluster.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch
ACCOUNT_ID=026951011097
CLUSTER_NAME=holdem-cluster

# ECS 클러스터 생성
aws ecs create-cluster \
  --cluster-name $CLUSTER_NAME \
  --region ap-northeast-2 \
  --profile $PROFILE
echo "ECS cluster created: $CLUSTER_NAME"

# ECS-optimized AMI 조회
ECS_AMI=$(aws ssm get-parameters \
  --names /aws/service/ecs/optimized-ami/amazon-linux-2023/recommended/image_id \
  --region ap-northeast-2 \
  --profile $PROFILE \
  --query 'Parameters[0].Value' --output text)
echo "Using AMI: $ECS_AMI"

# EC2 Security Group ID 조회
EC2_SG=$(aws ec2 describe-security-groups \
  --filters "Name=group-name,Values=holdem-ec2-sg" \
  --query 'SecurityGroups[0].GroupId' --output text \
  --profile $PROFILE)

# Launch Template 생성 (ECS 에이전트 자동 등록)
LT_ID=$(aws ec2 create-launch-template \
  --launch-template-name holdem-ecs-lt \
  --launch-template-data "{
    \"ImageId\": \"$ECS_AMI\",
    \"InstanceType\": \"t3.small\",
    \"SecurityGroupIds\": [\"$EC2_SG\"],
    \"IamInstanceProfile\": {\"Name\": \"holdem-ecs-instance-profile\"},
    \"UserData\": \"$(echo '#!/bin/bash
echo ECS_CLUSTER=holdem-cluster >> /etc/ecs/ecs.config' | base64 -w 0)\",
    \"TagSpecifications\": [{
      \"ResourceType\": \"instance\",
      \"Tags\": [{\"Key\": \"Name\", \"Value\": \"holdem-ecs-node\"}]
    }]
  }" \
  --profile $PROFILE \
  --query 'LaunchTemplate.LaunchTemplateId' --output text)
echo "Launch Template: $LT_ID"

# ASG 생성
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name holdem-ecs-asg \
  --launch-template "LaunchTemplateId=$LT_ID,Version=\$Latest" \
  --min-size 2 \
  --max-size 4 \
  --desired-capacity 2 \
  --vpc-zone-identifier "subnet-0dd262b44669174f8,subnet-04bea6a2e3d9da9f5,subnet-02e3f5e14bf2a7282" \
  --profile $PROFILE
echo "ASG created: holdem-ecs-asg"

# ECS Capacity Provider 생성
ASG_ARN=$(aws autoscaling describe-auto-scaling-groups \
  --auto-scaling-group-names holdem-ecs-asg \
  --profile $PROFILE \
  --query 'AutoScalingGroups[0].AutoScalingGroupARN' --output text)

aws ecs create-capacity-provider \
  --name holdem-ecs-cp \
  --auto-scaling-group-provider "{
    \"autoScalingGroupArn\": \"$ASG_ARN\",
    \"managedScaling\": {
      \"status\": \"ENABLED\",
      \"targetCapacity\": 80,
      \"minimumScalingStepSize\": 1,
      \"maximumScalingStepSize\": 2
    },
    \"managedTerminationProtection\": \"DISABLED\"
  }" \
  --region ap-northeast-2 \
  --profile $PROFILE
echo "Capacity Provider created: holdem-ecs-cp"

# 클러스터에 Capacity Provider 연결
aws ecs put-cluster-capacity-providers \
  --cluster $CLUSTER_NAME \
  --capacity-providers holdem-ecs-cp \
  --default-capacity-provider-strategy "[{\"capacityProvider\": \"holdem-ecs-cp\", \"weight\": 1}]" \
  --region ap-northeast-2 \
  --profile $PROFILE

echo "Done: ECS cluster ready"
```

- [ ] **Step 3: 실행**

```bash
chmod +x infra/ecs/02-cluster.sh && bash infra/ecs/02-cluster.sh
```

Expected 마지막 줄: `Done: ECS cluster ready`

- [ ] **Step 4: EC2 노드 등록 확인 (약 2~3분 대기)**

```bash
aws ecs list-container-instances \
  --cluster holdem-cluster \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'containerInstanceArns' \
  --output json
```

Expected: 2개 ARN 배열

- [ ] **Step 5: 커밋**

```bash
git add infra/ecs/02-cluster.sh
git commit -m "feat: add ECS cluster and ASG creation script"
```

---

### Task 8: Task Definition 등록

**Files:**
- Create: `infra/ecs/03-task-def.sh`

- [ ] **Step 1: Redis 엔드포인트 확인**

```bash
REDIS_HOST=$(aws elasticache describe-cache-clusters \
  --cache-cluster-id holdem-redis \
  --show-cache-node-info \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'CacheClusters[0].CacheNodes[0].Endpoint.Address' \
  --output text)
echo "REDIS_HOST=$REDIS_HOST"
```

Expected: `holdem-redis.xxxxxx.0001.apn2.cache.amazonaws.com`

- [ ] **Step 2: 03-task-def.sh 작성**

`infra/ecs/03-task-def.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch
ACCOUNT_ID=026951011097
REGION=ap-northeast-2

REDIS_HOST=$(aws elasticache describe-cache-clusters \
  --cache-cluster-id holdem-redis \
  --show-cache-node-info \
  --region $REGION \
  --profile $PROFILE \
  --query 'CacheClusters[0].CacheNodes[0].Endpoint.Address' \
  --output text)

# CloudWatch 로그 그룹 생성
aws logs create-log-group \
  --log-group-name /ecs/holdem-server \
  --region $REGION \
  --profile $PROFILE 2>/dev/null || true

# Task Definition 등록
aws ecs register-task-definition \
  --family holdem-server \
  --network-mode awsvpc \
  --requires-compatibilities EC2 \
  --cpu 256 \
  --memory 512 \
  --task-role-arn "arn:aws:iam::${ACCOUNT_ID}:role/holdem-ecs-task-role" \
  --execution-role-arn "arn:aws:iam::${ACCOUNT_ID}:role/holdem-ecs-execution-role" \
  --container-definitions "[
    {
      \"name\": \"holdem-server\",
      \"image\": \"${ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/holdem-server:latest\",
      \"portMappings\": [{\"containerPort\": 3001, \"protocol\": \"tcp\"}],
      \"essential\": true,
      \"environment\": [
        {\"name\": \"REDIS_HOST\", \"value\": \"${REDIS_HOST}\"},
        {\"name\": \"REDIS_PORT\", \"value\": \"6379\"},
        {\"name\": \"PORT\", \"value\": \"3001\"},
        {\"name\": \"HISTORY_BUCKET\", \"value\": \"holdem-history-026951011097\"},
        {\"name\": \"AWS_REGION\", \"value\": \"${REGION}\"}
      ],
      \"logConfiguration\": {
        \"logDriver\": \"awslogs\",
        \"options\": {
          \"awslogs-group\": \"/ecs/holdem-server\",
          \"awslogs-region\": \"${REGION}\",
          \"awslogs-stream-prefix\": \"ecs\"
        }
      },
      \"healthCheck\": {
        \"command\": [\"CMD-SHELL\", \"curl -f http://localhost:3001/health || exit 1\"],
        \"interval\": 30,
        \"timeout\": 5,
        \"retries\": 3,
        \"startPeriod\": 10
      }
    }
  ]" \
  --region $REGION \
  --profile $PROFILE \
  --query 'taskDefinition.taskDefinitionArn' --output text

echo "Done: Task Definition registered"
```

- [ ] **Step 3: 실행**

```bash
chmod +x infra/ecs/03-task-def.sh && bash infra/ecs/03-task-def.sh
```

Expected 마지막 줄: `arn:aws:ecs:ap-northeast-2:026951011097:task-definition/holdem-server:1`  
그 다음 줄: `Done: Task Definition registered`

- [ ] **Step 4: 커밋**

```bash
git add infra/ecs/03-task-def.sh
git commit -m "feat: add ECS task definition registration script"
```

---

### Task 9: ALB + Target Group + Listener 생성

**Files:**
- Create: `infra/ecs/04-alb.sh`

- [ ] **Step 1: 04-alb.sh 작성**

`infra/ecs/04-alb.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch

ALB_SG=$(aws ec2 describe-security-groups \
  --filters "Name=group-name,Values=holdem-alb-sg" \
  --query 'SecurityGroups[0].GroupId' --output text \
  --profile $PROFILE)

# ALB 생성
ALB_ARN=$(aws elbv2 create-load-balancer \
  --name holdem-alb \
  --subnets subnet-0dd262b44669174f8 subnet-04bea6a2e3d9da9f5 subnet-02e3f5e14bf2a7282 \
  --security-groups $ALB_SG \
  --region ap-northeast-2 \
  --profile $PROFILE \
  --query 'LoadBalancers[0].LoadBalancerArn' --output text)
echo "ALB ARN: $ALB_ARN"

# ALB 유휴 타임아웃 3600초 (WebSocket 유지)
aws elbv2 modify-load-balancer-attributes \
  --load-balancer-arn $ALB_ARN \
  --attributes Key=idle_timeout.timeout_seconds,Value=3600 \
  --region ap-northeast-2 \
  --profile $PROFILE

# Target Group 생성 (target-type: ip, awsvpc 모드)
TG_ARN=$(aws elbv2 create-target-group \
  --name holdem-tg \
  --protocol HTTP \
  --port 3001 \
  --vpc-id vpc-0e4283922b93ba6ea \
  --target-type ip \
  --health-check-path /health \
  --health-check-interval-seconds 30 \
  --healthy-threshold-count 2 \
  --unhealthy-threshold-count 3 \
  --region ap-northeast-2 \
  --profile $PROFILE \
  --query 'TargetGroups[0].TargetGroupArn' --output text)
echo "Target Group ARN: $TG_ARN"

# HTTP Listener 생성
aws elbv2 create-listener \
  --load-balancer-arn $ALB_ARN \
  --protocol HTTP \
  --port 80 \
  --default-actions "Type=forward,TargetGroupArn=$TG_ARN" \
  --region ap-northeast-2 \
  --profile $PROFILE
echo "Listener created"

# ALB DNS 출력
ALB_DNS=$(aws elbv2 describe-load-balancers \
  --load-balancer-arns $ALB_ARN \
  --region ap-northeast-2 \
  --profile $PROFILE \
  --query 'LoadBalancers[0].DNSName' --output text)
echo "ALB DNS: $ALB_DNS"
echo "Done: ALB ready"
```

- [ ] **Step 2: 실행**

```bash
chmod +x infra/ecs/04-alb.sh && bash infra/ecs/04-alb.sh
```

Expected 마지막 두 줄:
```
ALB DNS: holdem-alb-xxxxxxxx.ap-northeast-2.elb.amazonaws.com
Done: ALB ready
```

- [ ] **Step 3: 커밋**

```bash
git add infra/ecs/04-alb.sh
git commit -m "feat: add ALB and target group creation script"
```

---

### Task 10: ECS Service + Application Auto Scaling 생성

**Files:**
- Create: `infra/ecs/05-service.sh`

- [ ] **Step 1: 05-service.sh 작성**

`infra/ecs/05-service.sh`:
```bash
#!/bin/bash
set -e
PROFILE=dsu-roleswitch
REGION=ap-northeast-2
CLUSTER=holdem-cluster

EC2_SG=$(aws ec2 describe-security-groups \
  --filters "Name=group-name,Values=holdem-ec2-sg" \
  --query 'SecurityGroups[0].GroupId' --output text \
  --profile $PROFILE)

TG_ARN=$(aws elbv2 describe-target-groups \
  --names holdem-tg \
  --region $REGION \
  --profile $PROFILE \
  --query 'TargetGroups[0].TargetGroupArn' --output text)

# ECS Service 생성
aws ecs create-service \
  --cluster $CLUSTER \
  --service-name holdem-service \
  --task-definition holdem-server \
  --desired-count 2 \
  --launch-type EC2 \
  --network-configuration "awsvpcConfiguration={
    subnets=[subnet-0dd262b44669174f8,subnet-04bea6a2e3d9da9f5,subnet-02e3f5e14bf2a7282],
    securityGroups=[$EC2_SG],
    assignPublicIp=DISABLED
  }" \
  --load-balancers "targetGroupArn=$TG_ARN,containerName=holdem-server,containerPort=3001" \
  --deployment-configuration "minimumHealthyPercent=100,maximumPercent=200" \
  --region $REGION \
  --profile $PROFILE
echo "ECS Service created"

# Application Auto Scaling 등록
aws application-autoscaling register-scalable-target \
  --service-namespace ecs \
  --resource-id "service/${CLUSTER}/holdem-service" \
  --scalable-dimension ecs:service:DesiredCount \
  --min-capacity 2 \
  --max-capacity 8 \
  --region $REGION \
  --profile $PROFILE

# CPU 70% 스케일링 정책
aws application-autoscaling put-scaling-policy \
  --policy-name holdem-cpu-scaling \
  --service-namespace ecs \
  --resource-id "service/${CLUSTER}/holdem-service" \
  --scalable-dimension ecs:service:DesiredCount \
  --policy-type TargetTrackingScaling \
  --target-tracking-scaling-policy-configuration '{
    "TargetValue": 70.0,
    "PredefinedMetricSpecification": {
      "PredefinedMetricType": "ECSServiceAverageCPUUtilization"
    },
    "ScaleInCooldown": 300,
    "ScaleOutCooldown": 60
  }' \
  --region $REGION \
  --profile $PROFILE
echo "Auto Scaling policy attached"

echo "Done: ECS service running"
```

- [ ] **Step 2: 실행**

```bash
chmod +x infra/ecs/05-service.sh && bash infra/ecs/05-service.sh
```

Expected 마지막 줄: `Done: ECS service running`

- [ ] **Step 3: Task 실행 확인 (약 1~2분)**

```bash
aws ecs describe-services \
  --cluster holdem-cluster \
  --services holdem-service \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'services[0].{running:runningCount,desired:desiredCount,status:status}' \
  --output json
```

Expected: `{"running": 2, "desired": 2, "status": "ACTIVE"}`

- [ ] **Step 4: 헬스체크 확인**

```bash
ALB_DNS=$(aws elbv2 describe-load-balancers \
  --names holdem-alb \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'LoadBalancers[0].DNSName' --output text)

curl http://$ALB_DNS/health
```

Expected: `OK`

- [ ] **Step 5: WebSocket 연결 테스트**

```bash
npx wscat -c ws://$ALB_DNS
```

입력: `{"type":"list_rooms"}`

Expected: `{"type":"rooms","rooms":[]}`

`Ctrl+C`로 종료.

- [ ] **Step 6: 커밋**

```bash
git add infra/ecs/05-service.sh
git commit -m "feat: add ECS service and auto scaling script"
```

---

### Task 11: 프론트엔드 재빌드 & S3 배포

**Files:**
- 없음 (빌드 & 배포)

- [ ] **Step 1: ALB DNS 확인**

```bash
ALB_DNS=$(aws elbv2 describe-load-balancers \
  --names holdem-alb \
  --region ap-northeast-2 \
  --profile dsu-roleswitch \
  --query 'LoadBalancers[0].DNSName' --output text)
echo "ws://$ALB_DNS"
```

- [ ] **Step 2: 프론트엔드 빌드 (ALB WebSocket URL 주입)**

```bash
cd client
VITE_WS_URL="ws://$ALB_DNS" npm run build
```

Expected: `dist/` 디렉토리 생성

- [ ] **Step 3: S3 배포**

```bash
aws s3 sync dist/ s3://holdem-client-026951011097/ --delete \
  --profile dsu-roleswitch
```

Expected: 파일 업로드 완료

- [ ] **Step 4: 최종 검증 — 4탭 게임 테스트**

브라우저 탭 4개에서 아래 URL 접속:
```
http://holdem-client-026951011097.s3-website.ap-northeast-2.amazonaws.com
```

각각 다른 닉네임으로 같은 방에 입장 → 5초 카운트다운 → 게임 시작 확인.

- [ ] **Step 5: 커밋**

```bash
cd ..
git commit --allow-empty -m "chore: ECS deployment complete, frontend deployed to S3"
```

---

## 리소스 삭제 순서 (실습 완료 후)

```bash
PROFILE=dsu-roleswitch
REGION=ap-northeast-2

# 1. ECS Service 삭제
aws ecs update-service --cluster holdem-cluster --service holdem-service --desired-count 0 --region $REGION --profile $PROFILE
aws ecs delete-service --cluster holdem-cluster --service holdem-service --region $REGION --profile $PROFILE

# 2. ASG 삭제
aws autoscaling delete-auto-scaling-group --auto-scaling-group-name holdem-ecs-asg --force-delete --profile $PROFILE

# 3. ALB / Target Group 삭제
ALB_ARN=$(aws elbv2 describe-load-balancers --names holdem-alb --region $REGION --profile $PROFILE --query 'LoadBalancers[0].LoadBalancerArn' --output text)
TG_ARN=$(aws elbv2 describe-target-groups --names holdem-tg --region $REGION --profile $PROFILE --query 'TargetGroups[0].TargetGroupArn' --output text)
aws elbv2 delete-load-balancer --load-balancer-arn $ALB_ARN --region $REGION --profile $PROFILE
aws elbv2 delete-target-group --target-group-arn $TG_ARN --region $REGION --profile $PROFILE

# 4. ECS Cluster 삭제
aws ecs delete-cluster --cluster holdem-cluster --region $REGION --profile $PROFILE

# 5. ElastiCache 삭제
aws elasticache delete-cache-cluster --cache-cluster-id holdem-redis --region $REGION --profile $PROFILE

# 6. 보안 그룹 삭제 (EC2, Redis 먼저, ALB 마지막)
EC2_SG=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=holdem-ec2-sg" --query 'SecurityGroups[0].GroupId' --output text --profile $PROFILE)
REDIS_SG=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=holdem-redis-sg" --query 'SecurityGroups[0].GroupId' --output text --profile $PROFILE)
ALB_SG=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=holdem-alb-sg" --query 'SecurityGroups[0].GroupId' --output text --profile $PROFILE)
aws ec2 delete-security-group --group-id $EC2_SG --profile $PROFILE
aws ec2 delete-security-group --group-id $REDIS_SG --profile $PROFILE
aws ec2 delete-security-group --group-id $ALB_SG --profile $PROFILE
```
